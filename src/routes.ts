export const publicRoutes = ['/']

export const privateRoutes = ['/app']

export const authRoutes = ['/auth']

export const apiAuthPrefix = '/api/auth'

export const APP_HOME = '/app'

export const DEFAULT_APP_LOGIN_REDIRECT = '/auth'
